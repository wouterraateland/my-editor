import { createContext } from "react";

export const ColorModeContext = createContext(["light", {}]);
export const NodeContentContext = createContext({});
export const NotificationsContext = createContext([[], {}]);
